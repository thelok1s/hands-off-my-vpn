package dev.lok1s.handoffmyvpn.ui.theme
// Fallback palette used when dynamic (Monet) color is unavailable (pre-Android 12).
// expressiveLightColorScheme() / darkColorScheme() are used instead of manual seeds —
// these constants are kept only as reference if custom seeding is ever needed.
